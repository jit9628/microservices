spring:
  cloud:
    gateway:
      routes:
        - id: student-service
          uri: lb://student-service
          predicates:
            - Path=/students/**
        - id: course-service
          uri: lb://course-service
          predicates:
            - Path=/courses/**
